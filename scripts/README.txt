# Scripts

This project primarily uses **n8n workflows** for data processing and transformation.

Custom logic such as JSON formatting and binary file generation is implemented inside **n8n Code nodes**, so no standalone scripts are required.

This folder is included for extensibility if future automation scripts are needed.

